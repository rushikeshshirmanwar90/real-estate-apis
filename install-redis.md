# Redis Installation Commands

```bash
# Install Redis client and types
npm install redis @types/redis

# Optional: Install ioredis (alternative Redis client with better TypeScript support)
npm install ioredis @types/ioredis
```